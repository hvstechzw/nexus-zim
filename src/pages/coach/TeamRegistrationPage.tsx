import { PagePlaceholder } from "@/components/nash/PagePlaceholder";

export default function TeamRegistrationPage() {
  return (
    <PagePlaceholder
      title="Season Team Registration"
      tier="school"
      routeHint="src/pages/coach/TeamRegistrationPage.tsx"
      description="Submit team rosters per season per sport — eligibility-checked against the NASH athlete registry before submission."
    />
  );
}
