import { PagePlaceholder } from "@/components/nash/PagePlaceholder";

export default function AthletesRegistryPage() {
  return (
    <PagePlaceholder
      title="National Athlete Registry"
      tier="federation"
      routeHint="src/pages/admin/AthletesRegistryPage.tsx"
      description="Search and manage the national athlete registry — NASH ID generation, SS linking, duplicate detection."
    />
  );
}
