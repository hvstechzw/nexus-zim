import { PagePlaceholder } from "@/components/nash/PagePlaceholder";

export default function NashMembersPage() {
  return (
    <PagePlaceholder
      title="NASH Members Registry"
      tier="federation"
      routeHint="src/pages/admin/NashMembersPage.tsx"
      description="All elected/appointed NASH and NAPH members across every organisation, with role titles and terms."
    />
  );
}
