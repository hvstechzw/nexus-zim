import { PagePlaceholder } from "@/components/nash/PagePlaceholder";

export default function NashSeasonsPage() {
  return (
    <PagePlaceholder
      title="NASH Seasons"
      tier="federation"
      routeHint="src/pages/admin/NashSeasonsPage.tsx"
      description="Academic-year / term season management — active flags, registration deadlines and competition windows."
    />
  );
}
